# Buyer agent harness config. Copy to buyer_agent/.env (gitignored) and fill in.
# One target per prefix; run with buyer_agent/run.sh (see its header for usage).

LOCAL_SALES_AGENT_MCP_URL=http://localhost:8000/mcp/
LOCAL_SALES_AGENT_TOKEN=

# DEV_SALES_AGENT_MCP_URL=
# DEV_SALES_AGENT_TOKEN=

# PROD_SALES_AGENT_MCP_URL=
# PROD_SALES_AGENT_TOKEN=

# Shared by all targets
GEMINI_API_KEY=
# GEMINI_MODEL=
